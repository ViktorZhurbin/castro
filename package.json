{
	"name": "castro-monorepo",
	"version": "1.0.0",
	"private": true,
	"workspaces": [
		"castro",
		"packages/*",
		"website",
		"test-site"
	],
	"scripts": {
		"dev": "bun --filter website dev",
		"build": "bun --filter website build",
		"format": "biome format --write",
		"check": "bun format && bun run --parallel --filter '*' --if-present type-check build knip && bun test && bun run test:errors",
		"test:sites": "bun test ./test-site",
		"test:errors": "bun test test-errors",
		"loc": "bunx cloc castro/src/ --exclude-dir=messages --not-match-f '.test'",
		"loc-files": "bun run loc --by-file"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.11",
		"typescript": "^6.0.2"
	}
}
