{
  "name": "castro-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "core",
    "packages/*",
    "website",
    "tests/**/*"
  ],
  "scripts": {
    "prepare": "husky",
    "dev": "bun --filter website dev",
    "build": "bun --filter website build",
    "format": "oxfmt --check",
    "format:fix": "oxfmt",
    "lint": "oxlint --format=agent",
    "lint:fix": "oxlint --fix --format=agent",
    "check:code": "bun run --parallel --filter '*' --if-present type-check knip",
    "test:build": "bun run --parallel --filter '!@test-errors/*' --if-present build",
    "test:site": "bun test tests/site tests/site-srcdir",
    "test:errors": "bun test tests/errors",
    "test:errors:up": "UPDATE_SNAPSHOTS=1 bun test:errors",
    "test:all": "bun test",
    "verify": "bun lint && bun format && bun check:code && bun test:all && bun test:build",
    "loc": "bunx cloc core/src/ --exclude-dir=messages --not-match-f '.test'",
    "loc:files": "bun run loc --by-file"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@vktrz/castro": "workspace:*",
    "husky": "^9.1.7",
    "oxfmt": "^0.61.0",
    "oxlint": "^1.76.0",
    "preact": "^10.29.8",
    "typescript": "^6.0.2",
    "wrangler": "4.120.1"
  }
}
