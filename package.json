{
	"name": "castro-monorepo",
	"version": "1.0.0",
	"private": true,
	"workspaces": [
		"castro",
		"website",
		"test-sites/*"
	],
	"scripts": {
		"dev": "bun --filter website dev",
		"build": "bun --filter website build",
		"format": "biome format --write",
		"type-check": "bun --workspaces type-check",
		"check:castro": "bun run --filter @vktrz/castro knip",
		"check": "bun format && bun type-check && bun check:castro && bun test",
		"test:sites": "bun test",
		"loc": "bunx cloc castro/src/ --exclude-dir=messages",
		"loc-files": "bun run loc --by-file"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.11"
	}
}
